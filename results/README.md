# Results

`phase1_runs.jsonl` is the compact append-only run ledger. Session directories
retain resolved commands, raw logs, normalized metrics, summaries, and selected
configurations. The existing checkpoint directories preserve the original
upstream and baseline/adjusted evidence. Large model checkpoints remain ignored.
